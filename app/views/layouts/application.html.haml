!!!
%html
  %head
    %title
      humanweb -
      = yield :title
    = stylesheet_link_tag 'reset'
    = stylesheet_link_tag '960'
    = stylesheet_link_tag 'default'

    = javascript_include_tag 'jquery-1.6.4.min'
    = javascript_include_tag 'jquery.lettering-0.6.1.min'

  %body

    #page-header
      .page
        #biglogo
          .none human<b>web</b>

        - @highlight ||= request.fullpath

        %ul#nav-menu
          - if @highlight == root_path
            %li.active= link_to "WHAT WE DO", root_path
          - else
            %li= link_to "WHAT WE DO", root_path

          - if @highlight == team_path
            %li.active= link_to "THE TEAM", team_path
          - else
            %li= link_to "THE TEAM", team_path

          - if @highlight.start_with?(work_path)
            %li.active= link_to "OUR WORK", work_path
          - else
            %li= link_to "OUR WORK", work_path

          - if @highlight == contact_path
            %li.active= link_to "HIRE US", contact_path
          - else
            %li= link_to "HIRE US", contact_path
    #btm-line
    
    #white
      #bodyContainer.page
        = content_for?(:content) ? yield(:content) : yield

    #page-footer
      .page
        &copy; #{DateTime.now.year} Human Web Solutions LLC
